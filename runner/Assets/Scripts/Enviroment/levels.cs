using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class levels : MonoBehaviour
{
    /*
       public class GameObject[] sectors;
         public int zPos = 50;
       public bool creatSection=false;
       public int lvlnum;

       // Update is called once per frame
       void Update()
       {
           if(creatSection == false)
           {
               creatSection = true;
               Startcoroutine(Genlevel());
           }
       }
       IEnumerator Genlevel()
       {
           lvlnum = Random.Range(0, 3);
           Instantiate(sectors[lvlnum], new Vector3(0, 0, zPos), Quaternion.identity);
           zPos += 50;
           yield return new WaitForSeconds(2);
           creatSection = false;
       }*/

}
